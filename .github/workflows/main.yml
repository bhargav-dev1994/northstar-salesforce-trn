name: Salesforce CI/CD

on:
  push:
    branches:
      - Dev1
      - SIT
      - UAT

jobs:
  deploy:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18.x'

    - name: Install Salesforce CLI
      run: |
        npm install --global @salesforce/cli
        echo "Salesforce CLI version:"
        sf --version
        echo "Salesforce CLI location:"
        where sf

    - name: Download Salesforce Ant Migration Tool
      run: |
        echo "Downloading Salesforce Ant Migration Tool..."
        Invoke-WebRequest -Uri "https://resources.docs.salesforce.com/rel1/doc/en-us/static/pdf/SalesforceAnt.zip" -OutFile "salesforce-ant.zip"
        Expand-Archive -Path salesforce-ant.zip -DestinationPath .

    - name: Verify Download
      run: |
        if (!(Test-Path -Path "./salesforce-ant/lib/ant-salesforce.jar")) {
          Write-Error "Salesforce Ant Migration Tool download failed."
        }

    - name: Deploy to Salesforce
      run: |
        echo "Deploying to Salesforce..."
        $env:PATH += ";$env:APPDATA\npm"
        ant -lib ./salesforce-ant/lib -f build.xml deploy
      env:
        SF_USERNAME_DEV1: ${{ secrets.SF_USERNAME_DEV1 }}
        SF_PASSWORD_DEV1: ${{ secrets.SF_PASSWORD_DEV1 }}
        SF_SERVERURL_SANDBOX: ${{ secrets.SF_SERVERURL_SANDBOX }}
