<project name="Salesforce Deployment" default="deploy" basedir=".">
  <!-- Define properties for Salesforce CLI -->
  <property name="deploy.source" value="force-app/main/default"/>
  <property name="org.alias" value="target-org-alias"/>

  <!-- Target to deploy to Salesforce using sf -->
  <target name="deploy">
      <echo message="Deploying to Salesforce..."/>
      <exec executable="sf" failonerror="true" osfamily="windows">
          <arg line="deploy metadata -m ${deploy.source} -u ${org.alias}"/>
      </exec>
  </target>

  <!-- Target to retrieve from Salesforce using sf -->
  <target name="retrieve">
      <echo message="Retrieving from Salesforce..."/>
      <exec executable="sf" failonerror="true" osfamily="windows">
          <arg line="retrieve metadata -m ${deploy.source} -u ${org.alias}"/>
      </exec>
  </target>
</project>
